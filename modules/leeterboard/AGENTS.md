# AGENTS

Generates ranking metrics for hackers across bounty programs.

Important files:
- `build_leeterboard.py` - compute rankings
- `graphql_scraper.py` - gather data from the public endpoint
- `rank_window.py` - context window logic for leaderboard
