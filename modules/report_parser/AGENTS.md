# AGENTS

Parses raw HackerOne JSON or CSV into normalized objects.

- `parse_report.py` handles field extraction
- `validate_schema.py` ensures schema compliance
- Use `agent_notes.md` for open questions about report formats
