# AGENTS

This directory houses standalone utility scripts. Each script lives in its own subfolder with a README and agent_notes file.

- `populate_mock_data/` seeds the database with example entries.
- `sync_once/` performs a single HackerOne sync.

Add new scripts under their own folder following this pattern.
